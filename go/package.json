{
  "name": "lambda-performance-go",
  "scripts": {
    "prebuild": "npm run fmt && make clean",
    "build": "make build",
    "fmt": "prettier --write . && go fmt",
    "predeploy": "npm run build",
    "deploy": "sls deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "serverless": "^2.15.0"
  }
}
